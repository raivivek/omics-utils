#! /usr/bin/env python

import sys
import yaml


def jsontoyaml(file):
  with open(file, 'r') as f:
    _yaml = yaml.load(f)
    print(yaml.dump(_yaml, default_flow_style=False, indent=4))


if __name__ == '__main__':
  if len(sys.argv) < 2:
    print("Exiting. Please provide input.")
    sys.exit(1)
  jsontoyaml(sys.argv[1])
